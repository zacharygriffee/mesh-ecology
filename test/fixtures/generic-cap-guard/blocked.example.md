This fixture is intentionally scanned only by `test/misc/generic-cap-guard.test.js`.

It models a future adjacent-repo-specific cap that should not be introduced into
Mesh-owned code or docs:

`cap/platform/request-review/v1`

The generic alternative should be a concern-local cap such as
`cap/concern/request-review/v1` with `profile` and `producer` fields.
